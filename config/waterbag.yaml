# Waterbag defect dataset for Ultralytics YOLO training.
#
# Put the real dataset under datasets/waterbag, or pass another YAML with
# --data when running train_v8.py, train_yolo11.py, or benchmark scripts.
path: ../datasets/waterbag
train: images/train
val: images/val
test:

# Classes
names:
  0: anomaly
